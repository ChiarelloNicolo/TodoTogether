@using Supabase.Gotrue
@using Client = Supabase.Client
<h3>Login</h3>
@inject Client Supabase

<h4>You have to be logged in to access TodoTogether</h4>

<button @onclick="CreateUser">SignUp</button>
<button @onclick="LoginUser">Login</button>

@code {


    private async Task CreateUser()
    {
        var session = await Supabase.Auth.SignUp("chiarello.nicolo1@gmail.com", "testpassword123");
    }

    private async Task LoginUser()
    {
        var session = await Supabase.Auth.SignIn("chiarello.nicolo1@gmail.com", "testpassword123");
    }

}